<table>
  <tr>
    <td>From:</td>
    <td><input id="from_date" name="from_date" type="text" value="<%= @from_date.strftime('%m/%d/%Y') %>" /></td>
  </tr>
  <tr>
    <td>To:</td>
    <td><input id="to_date" name="to_date" type="text" value="<%= @to_date.strftime('%m/%d/%Y') %>" /></td>
  </tr>
</table>