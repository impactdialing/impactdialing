<div class="box" style="width:600px;">
	<%= render :partial => 'shared/boxtop', :locals=>{:title=>"#{@campaign.name} Dials Report"} %>
	<div class="box-content">


		<div class="table">
		    <table width="100%" border="0" cellspacing="0" cellpadding="0">
		      <tr>
		        <th width="300">Call status</th>
		        <th>Dials</th>
		        <th>Percentage</th>
		      </tr>

				<% @records.each do |r| %>
				<tr class="<%= cycle("odd","even") %>">
					<td>
					<% if r[1].blank? %>Unanswered
					<% elsif r[1]=="Call completed with success."%>
					Answered
					<% else %>
					<%= r[1] %>
					<% end %>
					</td>
					<td><%= r[0] %></td>
					<td><%= ((r[0].to_f/@total)*100).to_i %>%</td>
			</tr>
				<% end %>
		    </table>

		    <div class="pagging">
		   		<div class="left"><%= @total %> dials total</div>
		    </div>

		</div>

		<br/>

<!--  Old Overview Report with Penetration and Minutes

			<% if @totalvoters>0 %>
			<div class="table">
			    <table width="100%" border="0" cellspacing="0" cellpadding="0">
			      <tr>
			        <th width="300">Penetration</th>
			        <th>Numbers</th>
			        <th>Percentage</th>
			      </tr>

					<tr class="odd">
						<td>Remaining Numbers</td>
						<td><%= @voters_to_call.length %></td>
						<td><%=((@voters_to_call.length.to_f/@totalvoters.to_f)*100).to_i  %>%</td>
					</tr>
					<tr class="event">
						<td>Finished Numbers</td>
						<td><%= @voters_called.length %></td>
						<td><%=((@voters_called.length.to_f/@totalvoters.to_f)*100).to_i  %>%</td>
					</tr>
			    </table>

			</div>
			<br/>
			<% end %>

			<div class="table">
			    <table width="100%" border="0" cellspacing="0" cellpadding="0">
			      <tr>
			        <th width="300">Minutes</th>
			        <th>Minutes</th>
			      </tr>
					<tr class="event">
						<td>Lead Talk Time</td>
						<td><%= @talkmins %></td>
					</tr>
					<tr class="odd">
						<td>Caller Talk Time</td>
						<td><%= @callerMins  %></td>
					</tr>
			    </table>


			    <div class="pagging">
			   		<div class="left"><%= @callerMins + @talkmins%> total minutes</div>
			    </div>
			</div>


-->

	</div>
</div>

<h2>Report Date Range:</h2>
<% form_tag ("/client/report_overview?id=#{ params[:id]}") do %> 
  
<table>
	<tr>
		<td>From:</td>
		<td> <%= unobtrusive_date_text_picker_tag :from_date,@from_date %> </td>
	</tr>
	<tr>
		<td>
			To:</td>
		<td>  <%= unobtrusive_date_text_picker_tag :to_date,@to_date %> 
			<input type="submit" class="button" value="update" size="4">
			</td>
	</tr>
</table>
<% end %>
