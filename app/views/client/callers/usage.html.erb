<div class="content clearfix">
  <div role="main">
	<%= render :partial => 'shared/boxtop', :locals=>{:title=>"#{@caller.name} Usage"} %>
    <% form_tag(usage_client_caller_path(:id => params[:id]), :method => :get) do %>

    <%= select_tag 'campaign_id', options_from_collection_for_select(@campaigns, 'id', 'name', @campaign.try(:id)), {:include_blank => 'Total'} %>
    <div class="buttons"></p>
      <button type="submit">Switch campaign</button>
    </div>
	  <p>The utilization table shows how much time your callers actually spent on the phone. Each call is rounded to the nearest second.</p>
    <table>
      <%= render :partial => 'shared/utilization', :locals => {:report => @caller, :usage=> @caller_usage} %>
		</table>
		<p>The billable table shows how much time you are being billed for. Each call is rounded to the nearest minute. Your caller time is only billed when your callers dial in, instead of using a microphone on their computer.</p>
		<table>
      <tr>
        <th>Billable time</th>
        <th>Minutes</th>
      </tr>
      <tr>
        <td>Caller</td>
        <td><%= @caller_usage.caller_time %></td>
      </tr>
      <tr>
        <td>Leads</td>
        <td><%= @caller_usage.lead_time %></td>
      </tr>
      <tr>
        <td>Total</td>
        <td><%= @caller_usage.total_time %></td>
      </tr>
      
    </table>
  </div>
</div>

<h3>Date range:</h3>
  <%= render :partial => 'shared/date_picker' %>
  <div class="buttons">
    <button type="submit">Update</button>
  </div>  
<% end %>
