<fieldset>
  <%= hidden_field_tag :id, @caller.id, {:class=>"field"} %>
  <p>
    <label>Email</label>
    <%= f.text_field :email, {:class=>"field"} %>
  </p>

  <p>
    <label>Password</label>
    <%= f.text_field :password, {:class=>"field"} %>
  </p>
	<p>
		<label>Campaign</label>
		<%= f.select("campaign_id", @campaigns.try(:collect){|c| [ c.name, c.id ]}) %>
	</p>
	<p>Each caller using the web interface needs their own login. Two people can't log in as the same caller at the same time.</p>
</fieldset>