<h1>Billing</h1>
  <% if @subscription.cancelled? %>
    <p>You have cancelled your Subscription.</p>
  <% else %>
    <p>Your current plan is <%= @subscription.type %>.</p>
    <% if @subscription.per_agent? %>
      <p>Minutes Left:  <%= @subscription.available_minutes %></p>
        <p class="actions"><a href="/client/subscriptions/<%= @subscription.id%>" class="action primary confirm">Upgrade</a>
      <% if !@subscription.trial? %>
        <a href="/client/update_billing_quantity" class="action secondary">Change # of callers</a>
        <%= link_to 'Cancel subscription', {action: "cancel", id: @subscription.id}, method: "put",class: "action secondary", confirm: 'Are you sure you want to cancel your subscription?' %>      
        </p>
      <% end %>
    <% end %>

    <% if @subscription.per_minute? %>
      <p>Your current balance is <%= number_to_currency  %>.</p>
      <p class="actions"><a href="/client/add_to_balance" class="action secondary">Add to your balance</a></p>
      <p class="actions"><a href="/client/recharge" class="action secondary">Configure auto-recharge</a></p>
      <p class="actions"><a href="/client/update_billing" class="action secondary">Update billing information</a></p>
    <% end %>
  <% end %>
  


<% content_for :javascripts do %>
  <script type="text/javascript">    
    $(function($) {
      
      
  });
 </script>
<% end %>