<%= render :partial => 'shared/boxtop', :locals=>{:title=>"#{@campaign.name} Usage"} %>
<div class="content clearfix">
  <div role="main">
    <p>The utilization table shows how much time your callers actually spent on the phone. Each call is rounded to the nearest second.</p>

    <table>
      <%= render :partial => 'shared/utilization', :locals => {:report => @campaign} %>
		</table>
		
		<p>The billable table shows how much time you are being billed for. Each call is rounded to the nearest minute. Your caller time is only billed when your callers dial in, instead of using a microphone on their computer.</p>
	
		<table>
      <tr>
        <th>Billable time</th>
        <th>Minutes</th>
      </tr>
      <tr>
        <td>Callers</td>
        <td><%= @caller_time %></td>
      </tr>
      <tr>
        <td>Leads</td>
        <td><%= @lead_time %></td>
      </tr>
      <tr>
        <td>Transfers</td>
        <td><%= @transfer_time %></td>
      </tr>
      <tr>
        <td>Voicemails</td>
        <td><%= @voice_mail_time %></td>
      </tr>
      <tr>
        <td>Abandoned calls</td>
        <td><%= @abandoned_time %></td>
      </tr>
      
      <tr>
        <td>Total </td>
        <td><%= @total_time %></td>
      </tr>
      
    </table>
  </div>
</div>

<h3>Date range:</h3>
<% form_tag(usage_client_reports_url(:id => params[:id]), :method => :get) do %>
  <input type="hidden" name="id" value="<%= params[:id] %>" />
  <%= render :partial => 'shared/date_picker' %>
  <div class="buttons">
    <button type="submit">Update</button>
  </div>
  
<% end %>
