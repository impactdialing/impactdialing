get longest_conversation, best_conversation, best_wrapup, and best_dials from simulator

available_callers = number of callers on hold + number calls of length > best_conversation + number of callers on wrapup of length > best_wrapup - number of calls of length > than longest_conversation

ringing_lines = number of currently ringing lines

dials_to_make = ( best_dials * available_callers ) - ringing_lines