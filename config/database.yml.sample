# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
    adapter: mysql
    host: localhost
    database: impactdialing_dev
    username: root
    password: password
    timeout: 5000
    reconnect: true

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql
  host: localhost
  database: impactdialing_test
  username: root
  password: password

production2:
    adapter: sqlite3
    database: production2.db
    pool: 5
    timeout: 5000
    
production:
    adapter: mysql
    host: domU-12-31-39-0F-0C-04.compute-1.internal
    database: impactdialing_prod
    username: root
    password: p4mjo!
    timeout: 5000
    reconnect: true
    pool: 10
