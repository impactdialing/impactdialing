development:
    adapter: mysql2
    host: localhost
    database: impactdialing_dev
    username: root
    password:
    timeout: 5000
    reconnect: true

test:
  adapter: mysql2
  host: localhost
  database: impactdialing_test<%= ENV['TEST_ENV_NUMBER'] %>
  username: root
  password:
