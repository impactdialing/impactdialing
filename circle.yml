machine:
  ruby:
    version:
      2.0.0-p645

dependencies:
  post:
    - wget https://saucelabs.com/downloads/sc-4.3.13-linux.tar.gz
    - tar -xzf sc-4.3.13-linux.tar.gz
    - mv ./sc-4.3.13-linux $HOME/sc-bin

test:
  override:
    - $HOME/sc-bin/bin/sc --user $SAUCE_USERNAME --api-key $SAUCE_ACCESS_KEY --readyfile ~/sauce-is-hot:
        background: true
        parallel: true
    - while [ ! -e ~/sauce-is-hot ]; do sleep 1; done:
        parallel: true
    - USE_SAUCE='ie' bin/parallel_features:
        parallel: true
        files:
          - spec/features/customer_admin/campaigns/*
    #- DISABLE_SPRING=1 USE_SAUCE='ch' bundle exec rake sauce:spec concurrency=1:
    #    parallel: true
    #- DISABLE_SPRING=1 USE_SAUCE='ff' bundle exec rake sauce:spec concurrency=1:
    #    parallel: true
    #- DISABLE_SPRING=1 USE_SAUCE='sa' bundle exec rake sauce:spec concurrency=1:
    #    parallel: true
  post:
    - killall --wait sc  # wait for Sauce Connect to close the tunnel
